я добавил цвета в тему, они будут работать в формате:background: var(--v-tok-surface); ну и вот еще пример: var(--v-tok-tooltip-text);

это в другой библиотеке, где цвета, я их уже туда внес и оно будет работать вот так именно.

поэтому все твои приколы с цветами и твою реализацию мы заменяем на вот такую где используется, чтобы везде цвета работали корректно.

/// Цвет из палитры хоста: host-color(indigo, lighten5) -> var(--v-indigo-lighten5)
@function host-color($group, $variant: base) {
@return var(--v-#{$group}-#{$variant});
} УДАЛИТЬ ТК ВСЕ БАЗОВЫЕ ЦВЕТА И ТАК ЕСТЬ

@function tok-color($token) {
@return var(--v-tok-#{$token});
}

Это переделал на вот так

потому что base не нужен

получается набор для стилей, которые импортируются в мой глобальный styles.scss вот такой

@function tok-color($token) {
@return var(--v-tok-#{$token});
}

@mixin tok-button-color($token) {
.tok-root & {
color: tok-color($token);
}
}

// Фирменный градиент Тока. Направление — по диагонали, как в макете точки входа.
@mixin tok-gradient($angle: 135deg) {
background-image: linear-gradient($angle, #{tok-color(gradient-from)} 0%, #{tok-color(gradient-to)} 100%);
}

// Толщина полосы прокрутки. Одно место на всю папку: лента, поле ввода, таблицы.
$tok-scrollbar-size: 6px;

@mixin tok-thin-scrollbar($size: $tok-scrollbar-size) {
scrollbar-width: thin;
scrollbar-color: tok-color(border-strong) transparent;

&::-webkit-scrollbar {
width: $size;
height: $size;
}

&::-webkit-scrollbar-track {
background: transparent;
}

&::-webkit-scrollbar-thumb {
background-color: tok-color(border-strong);
border-radius: $size;
}

&:hover::-webkit-scrollbar-thumb {
background-color: tok-color(text-muted);
}
}

$tok-panel-min-width: 520px;

$tok-radius-lg: 20px;
$tok-radius-md: 16px;
$tok-radius-sm: 12px;

$tok-space-xs: 4px;
$tok-space-sm: 8px;
$tok-space-md: 16px;
$tok-space-lg: 24px;
$tok-space-xl: 32px;

$tok-z-overlay: 200;
$tok-z-panel: 201;
$tok-z-entry: 199;

$tok-duration-panel: 280ms;
$tok-easing-panel: cubic-bezier(0.22, 1, 0.36, 1);

это все у нас лежит в@ tne-ui/core



а вот компонент уже в @tne-ui/components

туда я отдельно просто пишу компонент в папке и stories к нему

нам не нужна реализация через installTok, он будет просто импортиться в лейауте и появится у всех спашек уже тк лейаут тянется из библиотеки

пока переделай компонент ток и сторис чтобы я заимпортил и оно смогло работать и как отдельный компонент при просмотре и когда я в лейаут уже сам просто заимпортирую

с учетом всех комментариев

а потом уже дальше будем отлаживать сам компонент уже на месте в лейауте
